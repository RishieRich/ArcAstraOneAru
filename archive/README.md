# Archive

This folder contains tracked historical or reference material that is not used at runtime.
Nothing here is imported, deployed, packaged, or treated as current product documentation.

Before restoring an archived file, verify that its assumptions still match `AGENTS.md`, the
constitution, baseline specs, migrations, and current code.

Current contents:

- `reference-images/` — historical screenshots, incident captures, and a duplicate root logo.
  Runtime logo assets remain under `connector/src/arq_connector/assets/` and `frontend/public/`.
